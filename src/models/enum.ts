export enum DefaultConfig {
    EXPRESSION_FLAG = 'igm',
    COLOR = '#4F4F4F',
    SECTION = 'hideCode',
}

export enum ConfigLabels {
    REGEX = 'regex',
    COLOR = 'color',
}

export enum StateLabel {
    HIDDEN = 'hideCode.isHidden',
}